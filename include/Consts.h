#ifndef CONSTS_H
#define CONSTS_H

const int TILES_X = 10;
const int TILES_Y = 8;
const int PIXELS_PER_TILE = 16;
const int PIXEL_SIZE = 5;
const int WINDOW_WIDTH = TILES_X * PIXELS_PER_TILE* PIXEL_SIZE;
const int WINDOW_HEIGHT = TILES_Y * PIXELS_PER_TILE * PIXEL_SIZE;

#endif